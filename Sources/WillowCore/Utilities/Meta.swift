import Foundation

let version = "willow version 0.1.0"
let usage = "usage: willow [-h] [-v] input_file"
let help =
  """
  \(version)

  \(usage)

  FLAGS
    -h, --help      Prints this message.
    -v, --version   Prints the Alba version number.
  """
